<h2>
<select class='plain-select' onchange={{action "filterChanged" value="target.value"}}>
  {{#each filterOptions as |option|}}
    <option value={{option.value}}  selected={{eq selectedFilter option.value}}>{{option.label}}</option>
  {{/each}}
</select>
Live Shots
</h2> 

<div class="col-md-2">

<ul class="list-unstyled">
  {{#each filteredLiveShots as |live-shot|}}
    <li>{{#link-to "live-shots.live-shot" live-shot }} {{live-shot.title}} {{/link-to}}</li>
  {{/each}}
    <li>{{#link-to "live-shots.new"}} +New Live Shot {{/link-to}}</li>
</ul>
</div>
<div class="col-md-9">
  {{!-- LiveShot --}}
  {{outlet}}
</div>
<div class="col-md-1">
</div>
